apiVersion: core.loom.io/v1alpha1
kind: NetworkFunctionConfig
metadata:
  name: pkt-logger-config
  namespace: default
spec:
  tables:
    MyIngress.log_table:
      entries:
        - matchFields:
          - name: hdr.inner_ipv4.src_addr
            type: Exact
            exact:
              ipv4Address: 10.123.0.3
          action:
            name: MyIngress.log